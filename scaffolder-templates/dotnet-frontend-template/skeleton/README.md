# ${{ values.component_id }}

${{ values.description }}

소스코드 repository의 위치는 [여기](https://${{ values.repo.host }}?owner=${{ values.repo.owner }}&repo=${{values.component_id}}) 입니다. 
dotnet 8.0 기반으로 애플리케이션을 빌드합니다.